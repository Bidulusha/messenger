# messenger
```
sudo apt install rustup
rustup update
```

```
rustup set default # or smth like that, just try cargo --version and get error, then set defaults
```

```
cargo install watch # for watching changes in rust files

cargo watch -x run #to all folders except shared-lib, on shared-lib type

cargo watch -x build
```
